<Page
    x:Class="GraphTest.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GraphTest"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:Name="Main_grid">
        <Grid ScrollViewer.HorizontalScrollBarVisibility="Visible" ScrollViewer.VerticalScrollBarVisibility="Visible">
            <Button x:Name="Button1" Content="Test" Margin="10,10,0,0" VerticalAlignment="Top" Height="32" Width="100" Click="Button1_Click"/>
            <Canvas x:Name="Base_canvas" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="#FF424242" Margin="0,64,0,0">
                <Canvas x:Name="Main_canvas" Width="256" Height="256" HorizontalAlignment="Center" VerticalAlignment="Center" Background="White" Canvas.Left="256" Canvas.Top="256"/>
                <TextBox x:Name="Text_ElapsedTime" Text="" HorizontalAlignment="Left" VerticalAlignment="Top" FocusVisualPrimaryBrush="{x:Null}" BorderBrush="{x:Null}" Background="{x:Null}" FocusVisualSecondaryBrush="{x:Null}" SelectionHighlightColor="#FF038387" IsReadOnly="True" AllowFocusOnInteraction="False"/>
            </Canvas>
        </Grid>

    </Grid>
</Page>
